from __future__ import annotations

import sys
from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parents[1]
if str(PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(PROJECT_ROOT))


from core.config import PathsConfig


HUMAN_TEXTS = [
    "Сегодня я снова опоздал на автобус, поэтому пришлось идти пешком до университета. По дороге встретил старого друга и мы обсудили планы на семестр.",
    "Вечером я готовил ужин и параллельно слушал лекцию. Конспект получился сумбурным, зато идеи для исследования стали понятнее.",
    "На выходных мы с семьей ездили за город. Был сильный ветер, но прогулка вдоль реки все равно получилась очень спокойной.",
    "Я долго редактировал этот абзац, потому что не мог подобрать точный пример. В итоге решил оставить простую формулировку.",
]

AI_WEAK_TEXTS = [
    "Данный текст представляет собой общее описание ситуации. Следовательно, можно сделать вывод о важности анализа. В заключение необходимо отметить значимость вывода.",
    "В современном мире технологии играют важную роль. Таким образом, развитие технологий является важным фактором прогресса общества и экономики.",
    "Текст можно рассматривать как систему элементов. Каждый элемент обладает функцией и целью, что делает систему более структурированной и эффективной.",
    "Необходимо подчеркнуть, что актуальность темы определяется многими причинами. Поэтому исследование имеет важное значение для дальнейшей практики.",
]

AI_STRONG_TEXTS = [
    "Если смотреть на проблему через призму повседневных решений, видно, что выбор редко бывает полностью рациональным. Мы скорее балансируем между привычкой, выгодой и эмоциональным комфортом.",
    "Интересно, что одинаковые данные могут приводить к разным выводам в зависимости от формулировки вопроса. Поэтому в исследовании важно фиксировать не только результат, но и контекст постановки задачи.",
    "Когда модель пишет убедительно, ошибку труднее заметить не из-за сложности темы, а из-за плавности аргументации. Читатель быстрее соглашается с логикой, если переходы между тезисами выглядят естественно.",
    "Практическая ценность эксперимента возрастает, когда мы измеряем устойчивость на разных длинах текста. Тогда видно, где алгоритм действительно извлекает стиль, а где опирается на случайные маркеры.",
]


def write_group(texts: list[str], folder: Path) -> None:
    folder.mkdir(parents=True, exist_ok=True)
    for i, text in enumerate(texts, start=1):
        (folder / f"sample_{i}.txt").write_text(text, encoding="utf-8")


if __name__ == "__main__":
    paths = PathsConfig()
    write_group(HUMAN_TEXTS, paths.data_raw / "human")
    write_group(AI_WEAK_TEXTS, paths.data_raw / "ai_weak")
    write_group(AI_STRONG_TEXTS, paths.data_raw / "ai_strong")
    print("Demo dataset created in data/raw")
